#!/bin/bash

function hide () { ##toHide(string- file name), hideIn (string-file name), seed (int - any integer, remember it for unlock!!), outFileBase(string- file name base)
	#determine number of lines to hide
	#multiply by 6
	#determine starting position from date of input file
	#determine multiple to hide on from seed
	#hide
	#fill in with dummy files with random dates and times as long as they do not clash with dates used for real file
}
hide "$1" "$2" "$3" "$4"